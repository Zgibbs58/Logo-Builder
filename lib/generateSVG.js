// const shapes = require("./shapes");

// function generateSVG(answers) {
//   // shapes[answers.shape] is accessing the shape object from shapes.js
//   return `<svg version="1.1" width="300" height="200" xmlns="http://www.w3.org/2000/svg">
// ${shapes[answers.shape].renderShape(shapes[answers.shape], answers.colorChoice.includes("hex") ? "#" + answers.shapeColor : answers.shapeColor)}

// <text x="150" y="140" font-size="60" text-anchor="middle" fill="${
//     answers.colorChoice.includes("hex") ? "#" + answers.textColor : answers.textColor
//   }">${answers.text.toUpperCase()}</text>
// </svg>`;
// }

// module.exports = generateSVG;
